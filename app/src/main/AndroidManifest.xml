<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buyer_map">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MapsActivityCurrentPlace"></activity>
        <activity android:name=".InformationB" />
        <activity android:name=".Information2" />
        <activity android:name=".CheckTitle" />
        <activity android:name=".Check" />
        <activity android:name=".Check2" />
        <activity android:name=".Buyer" />
        <activity android:name=".coordinates" />
        <activity android:name=".Crop2" />
        <activity android:name=".request" />
        <activity android:name=".Information" />
        <activity android:name=".Farm" />
        <activity android:name=".Crop" />
        <activity android:name=".Seller" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".Splash"
            android:label="@style/SplashTheme">
            <intent-filter>
                <action android:nam
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>e="android.intent.action.MAIN" />

        </activity>
        <activity android:name=".HomeActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDSISTzJdZYFiSQhJN96J4TJ4WqdL_d0y8" />


    </application>

</manifest>